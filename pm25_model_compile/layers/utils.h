#ifndef OPT_UTILS_H
#define OPT_UTILS_H

#include <math.h>
#include <stdio.h>

#define c_max(a, b) ((a) > (b) ? (a) : (b))
#define c_min(a, b) ((a) < (b) ? (a) : (b))
#define c_abs(a) ((a) >= 0.0 ? (a) : -(a)) 

#endif