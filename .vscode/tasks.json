{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build forward",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "${workspaceFolder}/model_compile/forward.c",
                "-o",
                "${workspaceFolder}/model_compile/forward",
                "-lm"
            ],
            "group": "build",
            "problemMatcher": [
                "$gcc"
            ]
        },
    ]
}